# This rule is not used by the project hyperfleet-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to hyperfleet.redhat.com resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: hyperfleet-operator
    app.kubernetes.io/managed-by: kustomize
  name: hyperfleetconfig-viewer-role
rules:
- apiGroups:
  - hyperfleet.redhat.com
  resources:
  - hyperfleetconfigs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - hyperfleet.redhat.com
  resources:
  - hyperfleetconfigs/status
  verbs:
  - get
